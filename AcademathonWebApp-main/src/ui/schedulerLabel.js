import React from 'react';
import './schedulerLabel.css'; // Import CSS for styling

function SchedulerLabel({ text, }) {
  return (
    <label>
      {text}
    </label>
  );
}

export default SchedulerLabel;